#' Prefix for consumption columns
#'
#' @return Prefix for consumption columns
consumption_prefx <- function() {
  return("consumption_")
}

#' Prefix for end percentage columns
#'
#' @return Prefix for end percentage columns
end_percentage_prefix <- function() {
  return("end_percentage_")
}

#' Prefix for columns with real temperatures
#'
#' @return Prefix for columns with real temperatures
real_temp_prefix <- function() {
  return("real_temp_")
}

#' Prefix for columns with predicted temperatures
#'
#' @return Prefix for columns with predicted temperatures
predicted_temp_prefix <- function() {
  return("pred_temp_")
}
